# =============================================================================
# MongoDB MCP Workshop – Environment Configuration
# =============================================================================
# Copy this file:  cp .env.example .env
# Then fill in the values for your LLM provider below.

# -----------------------------------------------------------------------------
# LLM Provider – pick ONE option and uncomment it
# -----------------------------------------------------------------------------

# --- Option A: AWS Bedrock ---------------------------------------------------
# Run with:  docker compose --profile bedrock up -d
#
# AWS_ACCESS_KEY_ID=AKIA...
# AWS_SECRET_ACCESS_KEY=...
# AWS_DEFAULT_REGION=us-east-1
# OPENAI_API_BASE_URLS=http://bedrock-gateway:8866/api/v1
# OPENAI_API_KEYS=bedrock

# --- Option B: Azure AI Foundry / Azure OpenAI --------------------------------
# Run with:  docker compose up -d
#
# OPENAI_API_BASE_URLS=https://<resource>.cognitiveservices.azure.com/openai/v1
# OPENAI_API_KEYS=<your-azure-api-key>

# -----------------------------------------------------------------------------
# MongoDB
# -----------------------------------------------------------------------------
# Default: local MongoDB inside Docker Compose
MONGODB_URI=mongodb://mongodb:27017/pos?replicaSet=rs0

# To use Atlas instead, comment the line above and uncomment below:
# MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/pos

# -----------------------------------------------------------------------------
# Open WebUI
# -----------------------------------------------------------------------------
WEBUI_SECRET_KEY=mcp-workshop-secret
